route:
  receiver: local
  group_by: [alertname, job]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

# Alerts are visible in the Alertmanager UI. Add organization-specific
# email/webhook receivers here without committing credentials.
receivers:
  - name: local
